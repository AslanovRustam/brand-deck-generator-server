{
  "name": "brand-deck-generator-server",
  "version": "1.0.0",
  "private": true,
  "description": "Локальный сервер: раздаёт генератор презентаций и проксирует запросы к OpenAI, чтобы ключ не светился в браузере",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
